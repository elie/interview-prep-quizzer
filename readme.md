# Interview Prep Quizzer

A short demo from the Building Your First AI App meetup.

## Installation

Make sure you create a ``.env`` file and add your `OPENAI_API_KEY` to it.

```bash
npm install
node index.js
```